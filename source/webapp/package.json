{
  "name": "webapp",
  "version": "1.0.0",
  "description": "(Amazon Rekognition Custom Brand Detection) web application to allow upload videos, monitor and view analyzed results",
  "main": "index.js",
  "private": true,
  "scripts": {
    "pretest": "npm install",
    "test": "echo \"no test\"",
    "build:clean": "rm -rf dist && mkdir -p dist",
    "build:copy": "cp -rv css images src favicon.ico index.html ./dist",
    "build": "npm-run-all -s build:clean build:copy",
    "zip": "cd dist && zip -rq"
  },
  "author": "aws-specialist-sa-emea",
  "license": "MIT-0",
  "dependencies": {},
  "devDependencies": {}
}
